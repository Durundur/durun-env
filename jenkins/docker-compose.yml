services:
    jenkins-controller:
        build: 
          context: ./controller
        image: jenkins-controller:${BUILD_DATE}
        container_name: jenkins-controller
        ports:
            - "8080:8080"
            - "50000:50000"
        env_file:
            - ./.env
